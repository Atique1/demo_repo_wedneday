# tuples = ordered, immutable, allows duplicate elements
# tuple can create with parnthesis and inside each element separated by ,
# for one element we need to add comma at the end
# creat a tuple from iterable (lists, etc) by using builtin function
# acces elements from a tuple
# change element in the tuples
# check if one element is in the tuples
# find out the number of elements from the tuples
# count the a specific number of elements from a tuples
